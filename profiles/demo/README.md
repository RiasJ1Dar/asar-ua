# demo profile

Minimal exact-string map for smoke tests. Real apps (e.g. Antigravity) need large
maps and often DOM injectors — see `antigravity-ua` for a full production pack;
this repo is the generic pipeline.
